export enum Screens {
  home = 'Home',
  info = 'Info',
  settings = 'Settings',
  cart = 'Cart',
}

export enum Navigators {
  bottomTabs = 'bottomTabs',
  authNavigator = 'authNavigator',
}

export enum Slices {
  user = 'user',
}
